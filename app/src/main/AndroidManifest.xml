<?xml version="1.0" encoding="utf-8"?><!--
    File: app/src/main/AndroidManifest.xml
    Project: HomeoGO
    Created: 03.okt.2025 11:20 (Rīga)
    ver. 1.2
    Purpose: Application manifest for HomeoGO app. Deklarē atļaujas priekš STT/TTS
             un piesaista galveno aktivitāti ar pielāgoto tēmu Theme.HomeoGO.
    Comments:
      - Permissions: INTERNET + RECORD_AUDIO (Azure Speech SDK).
      - Theme: atsauce uz @style/Theme.HomeoGO (definēts themes.xml).
      - MainActivity ir LAUNCHER.
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lv.mariozo.homeogo">

    <!-- Permissions required for Azure Speech SDK -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomeoGO">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
