<?xml version="1.0" encoding="utf-8"?><!--
    File: app/src/main/AndroidManifest.xml
    Project: HomeoGO (Android, Jetpack Compose + Material3)
    Created: 03.okt.2025 08:35 (Rīga)
    ver. 1.2
    Purpose: Application manifest for HomeoGO app. Declares permissions for
             microphone and network (needed by Azure STT/TTS).
    Comments:
      - Added INTERNET + RECORD_AUDIO permissions.
      - Theme: left as Compose Material3 base.
      - Activity: MainActivity as LAUNCHER.
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lv.mariozo.homeogo">

    <!-- Permissions required for Azure Speech SDK -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomeoGO">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
